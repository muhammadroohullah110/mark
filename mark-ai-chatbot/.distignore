# Files excluded from WordPress plugin ZIP builds
# These assets are loaded from CDN at runtime

# Source textures (baked into GLB — not used at runtime)
public/model/texture.png
public/model/blue.png
# robot.glb stays in ZIP (431KB) as fallback when no CDN configured

# Development files
.git
.github
.gitignore
.distignore
node_modules
tests
*.log
*.md
!readme.txt
